không retry, chỉ parse bằng thư viện sau khi dùng kĩ thuật lọc thủ công để lọc json:
Phương pháp:

Loại bỏ nhanh các phần tử nằm ngoài json bằng cách đếm ngoặc đến khi số đóng và mở bằng nhau và chỉ lấy những phần tử bên trong 2 ngoặc đầu - cuối để xử lý tiếp: 
hoặc nếu thấy scoring ở trước { đầu tiên thì lấy từ scoring, tự động thêm { vào trước scoring

Chuẩn hóa dấu cách và dấu câu: nếu sau dấu cách là một dấu cách khác hoặc dấu câu hoặc các dấu như -, _, .,\,..... thì cũng bỏ dấu cách đó đi.
trừ dấu ".", hai ngoặc gần nhau, các dấu còn lại nếu tồn tại trên 1 cái liên tiếp thì xóa hết chỉ giữ 1
các trường là 
        
        "factuality" 
        "clarity" 
        "logical_coherence" 
        "coverage" 
        "utility" 
        "consistency" 
        "feedback" 

kiểm tra trong các ngoặc kép:

nếu phát hiện một từ chứa đầy đủ các kí tự trong các trường này theo đúng thứ tự và không nhất thiết phải kề nhau, thì biến từ đó thành trường phù hợp, và loại bỏ tất cả các từ còn lại trong ngoặc kép đó, thực hiện xuống dòng.
nếu phát hiện giữa dấu : và , có một số thuộc 1 - 5, loại bỏ tất cả các phần tử còn lại trong khoảng : đến ,
ở ngoài 2 khu vực trên, chỉ giữ lại các dấu : , [] {} còn lại bỏ hết

Phần khó nhất là trong feedback, tìm các cụm từ tủ, nếu thấy xuất hiện, bỏ hết phần sau cho đến dấu "